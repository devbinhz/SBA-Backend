spring:
  datasource:
    url: jdbc:h2:mem:bookverse;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: none

  data:
    redis:
      host: localhost
      port: 6379

bookverse:
  admin:
    email: admin@bookverse.local
    password: ChangeMe123!
    full-name: BookVerse Admin
  security:
    jwt-secret: change-me-at-least-32-bytes-change-me
  vnpay:
    tmn-code: test-tmn
    hash-secret: test-hash-secret
    payment-url: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
    return-url: http://localhost/payment/success
